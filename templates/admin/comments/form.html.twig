{# Les commentaires" #}
{% extends '@EasyAdmin/crud/form_theme.html.twig' %}

{% block form %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}